{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="page-header">
            <h1><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Add a new entry</h1>
        </div>
        <div class="row">
            {{ include('default/_form.html.twig', { 'form': form }) }}
        </div>
    </div>
{% endblock %}


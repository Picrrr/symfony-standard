{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="page-header">
            <h1><span class="glyphicon glyphicon-edit" aria-hidden="true"></span> Edit an entry</h1>
        </div>
        <div class="row">
            {{ include('default/_form.html.twig', { 'form': form }) }}
        </div>
    </div>
{% endblock %}

